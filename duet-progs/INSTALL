Install Instructions

Building the utilities is just make ; make install. The programs go into
/usr/local/bin and the libraries go into /usr/local/lib.

The 'duet' command is available to interface with, and debug the framework.
